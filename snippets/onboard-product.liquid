{% comment %}Used for home page sections only when featured products is chosen but no content has been selected{% endcomment %}

{% for i in (1..prod_limit) %}
{% capture current %}{% cycle 1, 2, 3, 4, 5 %}{% endcapture %}
  {%- assign placeholder = 'product-' | append: current -%}
  <div class="featured__collection-product product-index{% unless is_carousel %} {{ prod_grid }}{% if mobile_1pr %} sm-span-12{% else %} sm-span-6{% endif %} auto{% endunless %}">
    <div class="onboard">
        <div class="reveal">
          {{ placeholder | placeholder_svg_tag }}
        </div>
      </a>
    </div>
    <div class="product-info">
      <div class="product-info-inner">
        <a href="#" title="Sample product">
          <span class="prod-title">Sample product {{ forloop.index }}</span>
        </a>
        <div class="price">
          9.99
        </div>
      </div>
    </div>
  </div>
  {% endfor %}
